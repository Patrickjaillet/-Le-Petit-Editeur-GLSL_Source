[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "shadertoy_engine"
version = "0.1.12"
requires-python = ">=3.9"

[tool.maturin]
module-name = "shadertoy_engine"
